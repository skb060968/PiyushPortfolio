/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // Keep defaults — no export, no basePath, no assetPrefix
  // This works for:
  // • Local dev
  // • Vercel
  // • Netlify
  // • Custom domains
}

module.exports = nextConfig
